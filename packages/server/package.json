{
  "name": "@somarl.com/server",
  "version": "0.1.0",
  "scripts": {
    "start": "if-env NODE_ENV=production ?? npm run prod || npm run dev",
    "prod": "nodemon ./src/server.ts",
    "dev": "nodemon ./src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@somarl.com/pipe": "0.1.0",
    "chalk": "^2.4.1",
    "dotenv": "^6.2.0",
    "if-env": "^1.0.4",
    "jsonwebtoken": "^8.4.0",
    "koa": "^2.6.2",
    "koa-bodyparser": "^4.2.1",
    "koa-cors": "^0.0.16",
    "koa-jwt": "^3.5.1",
    "koa-logger": "^3.2.0",
    "koa-router": "^7.4.0",
    "mongodb": "^3.1.10",
    "node-fetch": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
