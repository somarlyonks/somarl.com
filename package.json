{
  "name": "somarl.com",
  "homepage": "https://somarl.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "page": "cd page && npm start",
    "test": "react-scripts-ts test --env=jsdom",
    "ci": "react-scripts-ts test --ci --coverage --env=jsdom",
    "ci-jest": "jest --env=jsdom",
    "server": "cd server && npm start"
  },
  "devDependencies": {
    "@types/jest": "^23.0.0",
    "npm-run-all": "^4.1.3",
    "typescript": "^3.0.3"
  }
}
